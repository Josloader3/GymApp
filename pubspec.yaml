name: gym_app
description: "Control de tus workouts y ejercicios"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  flutter_dotenv: ^5.2.1
  firebase_core: ^3.13.0
  flutter_bloc: ^9.1.1
  equatable: ^2.0.7
  firebase_auth: ^5.5.3
  google_sign_in: ^6.3.0
  firebase_database: ^11.3.5
  fluttertoast: ^8.2.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  
flutter:
  uses-material-design: true

  assets:
    - .env